# Copy this file to .env and set real values before deployment.
# PORT is optional and defaults to 3000 in docker-compose.
PORT=3000

# Required application environment variables:
MANAGEMENT_PASSWORD=change-me
PGSTORE_DSN=******host:5432/database?sslmode=disable
